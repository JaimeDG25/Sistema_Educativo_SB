server:
  port: 8083   # puerto único para MaterialCurso-Server

spring:
  application:
    name: materialcurso-server
  config:
    import: "optional:configserver:http://localhost:8888"

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
